

---

## 一、项目进展概览

### 核心成果

| 方向         | 状态      | 关键产出                          |
| ---------- | ------- | ----------------------------- |
| 嘉立创 MCP 工具 | ✅ 完成    | 兼容 Claude Code/Codex/OpenCode |
| 芯阳知识库 MCP  | ✅ 完成    | 内部搜索 API 封装，跨平台支持             |
| 上下文压缩插件    | ✅ 完成    | WebUI 长对话 Token 节省 60%+       |
| 知识库图片召回方案  | 🔄 设计完成 | 三套方案，已完成技术选型                  |

---

## 二、本月重点工作

### 1. 嘉立创/LCSC 元器件 MCP 工具（6月）

#### 1.1 建设背景

在硬件开发、元器件选型和故障分析过程中，工程师经常需要查询元器件型号、规格参数、库存、价格、数据手册等信息。

#### 1.2 数据源摸底

已完成嘉立创/LCSC 相关公开接口的初步摸底：

1. **JLCPCB SMT 零件搜索接口**
   - 输入：keyword、pageSize、currentPage
   - 输出：C 编号、名称、型号、厂商、分类、库存、阶梯价格、数据手册链接

2. **LCSC 元器件详情接口**
   - 输入：productCode
   - 输出：元器件名称、型号、厂商、分类、库存、阶梯价格、技术参数

3. **数据手册获取能力**
   - 从详情返回结果中提取 pdfUrl 字段

#### 1.3 MCP 工具实现

实现 3 个 MCP 工具：

| 工具 | 功能 |
|------|------|
| `lcsc_search` | 按关键词搜索嘉立创/LCSC 元器件库 |
| `lcsc_detail` | 按 C 编号获取元器件详细规格 |
| `lcsc_datasheet` | 获取元器件数据手册 PDF 链接 |

#### 1.4 跨平台兼容

已完成对以下 AI 助手的适配：

| AI 工具 | 状态 |
|---------|------|
| Claude Code | ✅ 已验证 |
| Codex CLI | ✅ 已验证 |
| OpenCode | ✅ 已验证 |
| 芯阳 WebUI | ✅ 已部署 |

---

### 2. 芯阳知识库 MCP 工具（6月）

#### 2.1 功能设计

封装内部搜索 API，供 AI 编码助手调用。

| 工具 | 描述 |
|------|------|
| `knowledge_base_search` | 搜索芯阳内部产品参数、技术方案、制度、流程、项目和内部文档 |

#### 2.2 技术方案

- 语言：Node.js + TypeScript
- 协议：MCP (Model Context Protocol)
- 传输：stdio（便于被各类 AI 助手调用）

#### 2.3 一键安装

```bash
node scripts/install.cjs --api-url http://YOUR_HOST:5010/search/dify
```

支持 Linux/Mac/Windows 平台自动检测安装。

---

### 3. Async Context Compression 插件（6月）

针对 WebUI 平台开发的长对话上下文压缩插件，现已上线稳定运行。

#### 核心技术实现

**① 三级 Token 估算优化**

- **Fast 估算**：`_estimate_text_tokens()` ~0.01ms，误差±15%
- **Cached**：`@lru_cache`，O(1) 查表返回
- **Precise**：`tiktoken.o200k_base` ~0.1ms

设计效果：~80% 的场景只用 Fast 路径就能判断 Token 远低于阈值

**② 系统消息绝对保护**

三层保护确保 LLM 行为指令永不丢失：
- `keep_first` 只计数非系统消息 → 自动保留
- Gap 系统消息提取 → 以原始消息形式保留
- 强制裁剪兜底 → 提取并重新插入

**③ 原生工具输出裁剪**

- 工具输出字符数 > 600 则裁剪
- 替换为 `[Content collapsed]`，标记 `is_trimmed = True`

**④ 异步摘要生成 — 永不阻塞用户**

```
用户请求 → Inlet(~1ms) → LLM → 返回用户 → 后台 asyncio.create_task()
                              → Token 计算 → 生成摘要 → 保存数据库
```

**⑤ 摘要 Prompt 工程**

XML 结构化输出，包含 9 个预定义章节：

```xml
<working_memory>
  <current_goal>...</current_goal>
  <user_preferences>...</user_preferences>
  <persistent_context>...</persistent_context>
  <recent_progress>...</recent_progress>
  <tool_state>...</tool_state>
  <errors_and_warnings>...</errors_and_warnings>
  <open_loops>...</open_loops>
  <next_reply_guidance>...</next_reply_guidance>
</working_memory>
```
![[file-20260701212256165.png]]

---
![[file-20260630224245064.png|736]]
![[file-20260630224321867.png]]
### 4. 知识库图片召回方案（本周新进度）

针对现有知识库无法召回图片的问题，设计了三套技术方案。

#### 问题背景

当前知识库只能检索文本 chunk，无法返回对应的图片、截图、图纸。用户询问"查电芯装配流程图"时，只能返回文字描述，无法看到实际图片。

#### 方案一：文本检索 + 页面截图返回（最快上线）

**特点**：复用现有文本检索，根据命中结果返回对应页面截图。

**架构**：
```
文档入库阶段：
PDF → 文本切片 + embedding → Milvus 入库
     → 每页渲染 PNG → 保存 page_no → page_image_url 映射

查询阶段：
OpenWebUI Tool → /search/dify 文本检索
     → 返回 doc_address + page_no
     → 后端补充 page_image_url
     → OpenWebUI 展示文本 + 页面截图
```

**目录结构**：
```
/data/xinyang_knowledge/
  pdf/
    doc123.pdf
  pdf_pages/
    doc123/
      page_0001.png
      page_0002.png
  page_manifest/
    doc123.json
```

**返回格式**：
```json
{
  "results": [
    {
      "result_type": "text_with_page_image",
      "score": 0.83,
      "doc_name": "电芯装配SOP.pdf",
      "page_no": 7,
      "content": "电芯装配流程包括上料、扫码、焊接...",
      "page_image_url": "http://10.1.120.36:5010/pdf_pages/doc123/page_0007.png",
      "neighbor_page_image_urls": [
        "http://10.1.120.36:5010/pdf_pages/doc123/page_0006.png",
        "http://10.1.120.36:5010/pdf_pages/doc123/page_0007.png",
        "http://10.1.120.36:5010/pdf_pages/doc123/page_0008.png"
      ]
    }
  ]
}
```

**优点**：改动最小，最快上线；不需要升级 Milvus；不需要图像模型；用户看得到图

**缺点**：不是真正的视觉检索；依赖文本检索准确性；需要入库时保留 page_no

### 渲染示例


![PDF 原页面](https://tse4.mm.bing.net/th/id/OIP.g9RoeyBWZxtjV-_Ua-c2HAHaFA?rs=1&pid=ImgDetMain&o=7&rm=3)

![[file-20260621120219432.png]]


---

#### 方案二：OCR/Caption 转文本 + 文本检索

**特点**：把图片转成文字描述（caption），写入原有文本知识库，检索 caption 时返回对应图片 URL。

**架构**：
```
图片 / 页面截图
  → 保存到静态文件服务（得到 image_url）
  → VLM 生成 caption / OCR 提取文字
  → caption 做文本 embedding
  → caption + image_url + doc_id + page_no 一起写入 Milvus
  → 用 /search/dify 检索 caption，返回时带上 image_url
```

**核心原则**：图片 URL 在入库时生成，而不是检索时凭空得到的

**Milvus Schema**：
```python
{
  "id": "doc123_p0007_img0001",
  "content": "这是一张电芯装配流程图，展示上料、扫码、焊接、检测、下料等步骤。",
  "embedding": [0.01, 0.02, "..."],
  "image_url": "/kb-images/doc123/doc123_p0007_img0001.png",
  "doc_address": "/pdf/doc123.pdf",
  "doc_name": "电芯装配SOP.pdf",
  "doc_id": "doc123",
  "page_no": 7,
  "category": "流程图",
  "content_type": "image_caption"
}
```

**推荐接口**：`/search/image_caption`

**优点**：实现简单；旧文本检索完全复用；可搜性提升

**缺点**：Caption 质量不稳定；视觉结构信息丢失；不是真正的视觉检索

---
#### 三套方案对比

| 对比项             | 方案一：文本+截图 | 方案二：OCR/Caption |
| --------------- | --------- | --------------- |
| **工程复杂度**       | 低 ⭐       | 中 ⭐⭐            |
| **上线速度**        | 快         | 中               |
| **本质**          | 文本命中→返回截图 | 把图翻译成文字再搜       |
| **检索对象**        | 文本 chunk  | 文字描述 caption    |
| **入库成本**        | 低，PDF渲染   | 中，VLM生成caption  |
| **检索成本**        | 低         | 低               |
| **需要升级 Milvus** | 否         | 否               |
| **需要 GPU**      | 否         | 入库时需要           |
| **适合场景**        | 任何带文本的文档  | 简单图/流程图/照片      |


---

## 三、下一步计划

| 优先级    | 方向        | 预期收益                |
| ------ | --------- | ------------------- |
| **P0** | 图片召回方案一落地 | 最快上线，解决 SOP/图纸图片召回  |
| **P1** | 图片召回方案二原型 | 验证 Caption 质量，评估覆盖率 |


---
