# 📋 组会周报

---

## ✨ 一、关键进展

### 🔌 Async Context Compression — Open WebUI 上下文压缩 Filter 插件

> 针对 Open WebUI 平台开发的长对话上下文压缩插件。通过智能摘要和消息压缩降低 Token 消耗，同时保持多轮对话连贯性。已发布至 GitHub 开源。

| 📌 属性       | 说明                                                    |
| :---------- | :------------------------------------------------------ |
| 🏠 **项目地址** | https://github.com/Fu-Jie/openwebui-extensions          |
| 📦 **版本**   | v1.6.5                                                   |
| 🛠️ **技术栈** | Python + Open WebUI Filter API + SQLAlchemy + tiktoken   |
| 📝 **许可**   | MIT                                                      |

---

### 🏗️ 架构总览

```mermaid
flowchart TB
    subgraph 用户["🧑 用户"]
        A[发送消息]
    end

    subgraph OpenWebUI["WebUI Platform"]
        B[Filter Pipeline]
    end

    subgraph 插件["Async Context Compression"]
        direction TB
        C["Phase 1: Inlet<br/>请求前处理<br/>• 加载历史摘要<br/>• 组装压缩上下文<br/>• 发送给 LLM"]
        D["Phase 2: Outlet<br/>响应后处理<br/>• 异步计算 Token<br/>• 判断是否触发压缩<br/>• 后台生成摘要"]
    end

    subgraph 存储["持久化层"]
        E[("chat_summary 表<br/>WebUI 共享数据库")]
    end

    subgraph 摘要模型["摘要 LLM"]
        F[指定模型<br/>如 gemini-flash]
    end

    A --> B
    B --> C
    C -->|压缩后的消息| G[LLM 生成响应]
    G --> D
    D -->|超过阈值| F
    F -->|生成摘要| D
    D -->|保存摘要| E
    E -->|加载已有摘要| C
```
#### 共享数据库引擎发现

```mermaid
flowchart TD
    Start[插件启动] --> Try1[尝试 get_db_context]
    Try1 -->|成功| GetBind[从 session 获取 bind]
    Try1 -->|失败| Try2[尝试 db_module.engine]
    Try2 -->|成功| GetBind
    Try2 -->|失败| Try3[尝试 ENGINE / BIND 等属性]
    Try3 -->|成功| GetBind
    Try3 -->|失败| Fallback[sessionmaker 兜底]
    GetBind --> CreateTable[创建 chat_summary 表<br/>若不存在]
    Fallback --> CreateTable
    CreateTable --> CheckSchema[发现 schema 名称<br/>多租户环境适配]
    CheckSchema --> Done[就绪]
```

**数据库表结构：**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | INTEGER PK | 自增主键 |
| `chat_id` | VARCHAR(255) UNIQUE | 聊天会话 ID（唯一索引） |
| `summary` | TEXT | 摘要内容 |
| `compressed_message_count` | INTEGER | 已压缩到的原始消息数 |
| `created_at` | DATETIME | 创建时间 |
| `updated_at` | DATETIME | 更新时间 |

---

### ⚙️ 核心技术实现

---

#### 1. 三级 Token 估算优化

避免每轮对话都调用 tiktoken（有 Python 调用开销），设计了三级估算路径：

```mermaid
flowchart TD
    Start[需要计算 Token] --> Q1{字符串是否<br/>缓存命中?}
    Q1 -->|是| Cached["② Cached<br/>@lru_cache<br/>O(1) 查表返回"]
    Q1 -->|否| Fast["① Fast 估算<br/>_estimate_text_tokens()<br/>~0.01ms, 误差±15%"]
    Fast --> Q2{估算值 ><br/>阈值的 85%?}
    Q2 -->|否| FastResult[使用估算值]
    Q2 -->|是| Precise["③ Precise 精确计算<br/>tiktoken.o200k_base<br/>~0.1ms"]
    Precise --> Q3{同一文本<br/>再出现?}
    Q3 -->|是| Cached
    Q3 -->|否| PreciseResult[使用精确值]
```

**Fast 估算原理（基于字符特征，无需 tokenizer）：**

| 文本类型                 | 估算策略                                                             |
| :------------------- | :--------------------------------------------------------------- |
| **纯 ASCII**（英文/代码）   | 按字符数 × 系数 0.17~0.20，额外考虑空格、标点和换行                                 |
| **含非 ASCII**（中文/日文等） | 采样前 256 个非 ASCII 字符，识别文字类型分布，按预设系数加权                             |
| **文字类型系数**           | 汉字 0.295 / 假名 0.235 / 谚文 0.175 / 西里尔 0.13 / 阿拉伯 0.135 / 泰文 0.145 |

**设计效果：** ~80% 的场景只用 Fast 路径就能判断 Token 远低于阈值，避免 tiktoken 开销。接近阈值时才降级精确计算。

---

#### 2. 系统消息绝对保护

系统消息是 LLM 行为指令的核心载体，三层保护确保永不丢失：

```
消息序列:  [sysA] [user1] [sysB] [user2] [user3] ... [userN-1] [userN]
               ↑                                      ↑            ↑
          keep_first=2                         保留结尾(keep_last=2)
          非系统消息计数: user1, user2

  ① keep_first 只计数非系统消息
     → 返回 index=4，包含 [sysA, user1, sysB, user2]（sys 自动保留）

  ② Gap 系统消息提取
     → [user3]...[userN-3] 之间的 sys 消息 → 以原始消息形式保留

  ③ 强制裁剪兜底
     → 丢弃原子组时，组内的 sys 消息 → 提取并重新插入最终输出

最终输出: [sysA] [user1] [sysB] [user2] + [摘要] + [保留的Gap sys] + [userN-1] [userN]
```

---
#### 3. 原生工具输出裁剪

```mermaid
flowchart TD
    A[遍历原子分组] --> B{工具输出字符数<br/>> 600?}
    B -->|否| C[保留完整内容]
    B -->|是| D[裁剪工具输出]
    D --> E["tool content → '[Content collapsed]'"]
    E --> F["assistant 前追加<br/>'[Tool outputs trimmed]'"]
    F --> G["标记 metadata<br/>is_trimmed = True"]
```

---

#### 4. 异步摘要生成 — 永不阻塞用户

```mermaid
sequenceDiagram
    participant User as 用户
    participant Plugin as 插件
    participant LLM as 对话 LLM
    participant Bg as 后台任务
    participant Summary as 摘要 LLM

    User->>Plugin: 请求
    Plugin->>Plugin: Inlet（~1ms）
    Plugin->>LLM: 请求
    LLM-->>Plugin: 响应
    Plugin-->>User: 返回

    Note over Plugin: 响应已返回，用户无感知
    Plugin->>Bg: asyncio.create_task()
    Bg->>Bg: Token 计算
    Bg->>Summary: 生成摘要
    Summary-->>Bg: 摘要
    Bg->>Plugin: 保存到数据库
```

---

#### 5. 摘要 Prompt 工程

摘要 Prompt 设计为 **XML 结构化输出**，包含 9 个预定义章节：

```xml
<working_memory>
  <current_goal>...</current_goal>
  <user_preferences>...</user_preferences>
  <persistent_context>...</persistent_context>     ← 保守使用
  <recent_progress>...</recent_progress>           ← 优先放这里
  <tool_state>...</tool_state>
  <errors_and_warnings>...</errors_and_warnings>
  <open_loops>...</open_loops>
  <next_reply_guidance>...</next_reply_guidance>
</working_memory>
```

**三种压缩风格对比：**

| 风格             | Token 消耗 | 适用场景          |
| :------------- | :------- | :------------ |
| `aggressive`   | 最低       | 极长对话、低成本推理模型  |
| `balanced`（默认） | 中等       | 通用场景          |
| `faithful`     | 较高       | 复杂推理、多项备选方案评估 |


**Prompt 元指令要点：**
- 旧摘要通过 `<previous_working_memory>` 传入 → **合并更新**而非替换
- `<persistent_context>` 遵循**保守原则**（不确定时放 `<recent_progress>`）
- 区分"已验证事实" vs "推断结论" vs "开放问题"
- 代码、路径、配置值、错误字符串要求**逐字保留**

---

### 📊 压缩效果

| 指标 | 压缩前 | 压缩后 | 节省 |
|:---|:---|:---|:---|
| 消息数（20条, keep_first=0, keep_last=6） | 20 条 | 7 条 | **≈ 65%** |
| Token 消耗 | 全量 | 受控在阈值内 | — |

---

### 🔧 关键配置

| 参数 | 默认值 | 说明 |
|:---|:---|:---|
| `compression_threshold_tokens` | 64000 | 触发压缩的 Token 阈值 |
| `max_context_tokens` | 128000 | 上下文硬限制 |
| `model_thresholds` | "" | 按模型单独设阈值，如 `gpt-4:8000:32000` |
| `keep_first` | 0 | 保留前 N 条非系统消息 |
| `keep_last` | 6 | 保留后 N 条完整消息 |
| `compression_style` | balanced | 压缩风格 |
| `summary_model` | None | 指定摘要模型（推荐 gemini-flash） |
| `enable_tool_output_trimming` | true | 工具输出裁剪 |
| `tool_trim_threshold_chars` | 600 | 裁剪阈值（字符） |
| `summary_fail_mode` | silent | 摘要失败：silent / raise |

---

## ⚠️ 二、风险与问题

| 风险 | 描述 | 应对措施 |
|:---|:---|:---|
| 🔒 **摘要模型兼容性** | Pipe 模型可能不支持标准生成接口 | 提供 `summary_model` 指定独立模型 |
| 🖼️ **图片 Token 不计入** | 多模态图片 Token 未纳入预算 | 文档提示按模型差异规划阈值 |
| ⚡ **首次压缩延迟** | 首达阈值时后台摘要需几秒 | 异步执行，用户无感知 |
| 🗑️ **原子组丢弃** | 极端长上下文中最旧组被丢弃 | 遵循优先级：sys > 引用 > 工具 > 普通 |
| 🔄 **版本依赖** | Open WebUI 新版本 API 变动 | 版本感知兼容层隔离差异 |

---

## 📅 三、下周计划

- [ ] 收集社区反馈，修复跨版本兼容性问题
- [ ] 研究将图片 Token 纳入预算计算
- [ ] 补充单元测试套件
- [ ] 优化摘要 Prompt，减少工具密集型场景的信息丢失
- [ ] 编写英文技术博客，总结长对话压缩最佳实践
